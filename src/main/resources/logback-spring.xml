<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Import Spring Boot's colour-conversion rules so %clr works below -->
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <!-- Human-readable console appender (default, i.e. json-logs profile is NOT active) -->
    <springProfile name="!json-logs">
        <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

    <!-- Structured JSON console appender (activate profile: json-logs) -->
    <!-- MDC fields including requestId are included automatically by LogstashEncoder -->
    <springProfile name="json-logs">
        <appender name="CONSOLE_JSON" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <fieldNames>
                    <timestamp>timestamp</timestamp>
                    <message>message</message>
                    <logger>logger</logger>
                    <thread>thread</thread>
                    <level>level</level>
                    <levelValue>[ignore]</levelValue>
                </fieldNames>
            </encoder>
        </appender>
        <root level="INFO">
            <appender-ref ref="CONSOLE_JSON"/>
        </root>
    </springProfile>

</configuration>
